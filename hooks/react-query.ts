import { User } from '@/types/user';
import { useQuery } from '@tanstack/react-query';

//TODO: wireup everything up.
const useUserData = (userId?: User['id']) => {

    if(userId == null) {

    }
}
